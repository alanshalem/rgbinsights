// Shared Tailwind class strings so a control's look is defined once.

/** Toolbar inputs/selects (search box, order/status pickers, event picker). */
export const INPUT_CLS =
  'rounded-lg border border-[var(--color-border)] bg-[var(--color-panel)] px-3 py-1.5 text-sm outline-none';
